{
  "name": "csc-metadata-checker",
  "version": "0.2.0",
  "description": "Scans metadata and detects problems in charts made for Clone Hero",
  "bin": "index.js",
  "main": "index.js",
  "scripts": {
    "build": "pkg . --targets=node8-win-x64 -o dist/build.exe",
    "build:mac": "pkg . --targets=node8-macos-x64 -o dist/build"
  },
  "author": "Paturages",
  "license": "GPL-3.0",
  "dependencies": {
    "iconv-lite": "^0.4.24",
    "midifile": "^2.0.0",
    "rimraf": "^2.6.3"
  },
  "devDependencies": {
    "pkg": "^4.4.0"
  }
}
